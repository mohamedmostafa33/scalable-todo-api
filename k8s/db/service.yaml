apiVersion: v1
kind: Service
metadata:
  name: todo-db-service
  namespace: todo-app
spec:
  selector:
    app: todo-db
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
  clusterIP: None