apiVersion: v1
kind: Service
metadata:
  name: todo-api-service
  namespace: todo-app
spec:
  selector:
    app: todo-api
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP